'use strict';
var nat = require('./nat');
var nbr = require('./nbr');
var vir = require('./vir');
var vre = require('./vre');
var common = require('./common');
var code = require('./code');
var num = require('./number-words');

module.exports = {
    all: {
        nat,
        nbr,
        vir,
        vre,
        common,
        code,
        num
    }
}
